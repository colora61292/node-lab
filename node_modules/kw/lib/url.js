var config = require('./application').config();

/**
 * get url by path info.
 *
 * @return {String}
 * @api public
 */

exports.getUrlByPathInfo = function (pathInfo) {
    for(var url in config.route){
        if(pathInfo == config.route[url]){
            return url;
        }
    }
    return '';
};
