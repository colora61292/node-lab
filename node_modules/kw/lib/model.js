var Model = function(model){
    this.title = 'Title';
};

Model.prototype.initByJSON = function(json){
    if(json){
        for(var key in json){
            this[key] = json[key];
        }
    }
}

module.exports = Model;
